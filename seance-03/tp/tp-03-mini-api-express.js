/**
 * TRAVAIL PRATIQUE 03 - MINI API EXPRESS
 *
 * Objectif : créer une petite API Express avec quelques routes simples.
 *
 * Pré-requis :
 * - Projet initialisé avec "npm init -y"
 * - "type": "module" ajouté dans package.json
 * - Express installé : npm install express
 *
 * Consignes :
 * 1. Créez un fichier "app.js" qui :
 *    - importe express (import express from 'express';)
 *    - crée une application Express
 *    - écoute sur le port 3000
 *
 * 2. Ajoutez les routes suivantes :
 *    - GET /              → renvoie un texte "Bienvenue sur l'API TP03"
 *    - GET /time          → renvoie un JSON avec l'heure actuelle
 *    - GET /hello/:name   → renvoie "Bonjour [name]"
 *
 * 3. Ajoutez un middleware logger (console.log de la méthode et de l'URL)
 *
 * 4. Testez vos routes dans le navigateur ou avec un outil (Postman, Thunder Client...).
 *
 * 5. Ajoutez un script "start" dans package.json :
 *    "start": "node app.js"
 */

// Ce fichier ne contient que l'énoncé. Le code est à écrire dans votre projet.

